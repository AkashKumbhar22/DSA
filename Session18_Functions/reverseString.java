public class reverseString {
    public static boolean reverseAndCheckPalindrome(String v){
        for(int i = 0; i<= v.length() ; i++){
            for(int j = v.length()-1 ; j > 0 ; j--)
                if(v.charAt(i) == v.charAt(j)){
                }
        }
        return boolean;
    }
}
